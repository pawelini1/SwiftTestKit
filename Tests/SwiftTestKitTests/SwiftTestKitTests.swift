    import XCTest
    @testable import SwiftTestKit

    final class SwiftTestKitTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(SwiftTestKit().text, "Hello, World!")
        }
    }
